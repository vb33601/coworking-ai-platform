FROM python:3.11-slim

WORKDIR /app

COPY services/search/requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY services/search/app ./app

ENV PYTHONPATH=/app
ENV PORT=8001

EXPOSE 8001

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8001"]
